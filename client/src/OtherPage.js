import React from "react";
import { Link } from "react-router-dom";

export default () =>{
    return (
        <div>
            I'm the other page. Go back to <Link to="/">Home</Link>
        </div>
);};
